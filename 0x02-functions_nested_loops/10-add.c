#include <stdlib.h>
#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: argument passed into the function
 * @b: second argument passed into the function
 * Return: Returns 0 (Success)
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
	return (0);
}
