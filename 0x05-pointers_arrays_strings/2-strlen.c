#include "main.h"
#include <string.h>

/**
 * _strlen - Returns the length of a string
 * @s: pointer to a char type
 * Return: Returns length of the string
 */

int _strlen(char *s)
{
	strlen(s);

	return (strlen(s));
}
