#include "main.h"

/**
 * more_numbers -  prints 10 times the numbers, from 0 to 14
 * @a: While loop variable
 * @b: For loop variable
 * Return: nothing
 */

void more_numbers(void)
{
	int a = 0;
	int b;

	while (a <= 9; a++)
	{
		for (b >= 0; b <= 14; b++)
		{
			_putchar(b);
		}
		_putchar('\n');
	}
	_putchar('\n');
}
