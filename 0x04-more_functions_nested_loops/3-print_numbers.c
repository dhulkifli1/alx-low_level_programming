#include "main.h"
#include <stdio.h>

/**
 * print_numbers - prints numbers
 * @a: character used in loop
 * Return: Returns nothing
 */

void print_numbers(void)
{
	char a = 0;
	
	while (a <= '9'; a++)
	{
		_putchar(a);
	}
	_putchar('\n');
}
